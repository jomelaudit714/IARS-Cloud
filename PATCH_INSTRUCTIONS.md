# IARS V4.4.94 Patch Instructions

Replace only these files in the current GitHub repository:

1. `app.py`
2. `iars_parser.py`

Then commit the changes, reboot the Streamlit app, and perform a hard refresh (`Ctrl + F5`).

No Supabase SQL, requirements, Master Data, or PDF editor update is required.
