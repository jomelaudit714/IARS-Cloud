# Deploy v4.0.1

Replace all files from this package in the GitHub repository and commit them together.
The essential visual hotfix files are:

- `iars_theme.py`
- `iars_auth.py`
- `assets/internal_audit_visual.png`
- `app.py`

Keep the exact folder paths. Reboot the Streamlit app after the commit and hard-refresh the browser (`Ctrl + F5`).
No new Supabase SQL migration is required.
